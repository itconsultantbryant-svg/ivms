/** Store identity used across the app, invoices, and receipts. */
export const BRAND_NAME = "Drop & Shop";

/** Logo bundled with the app (same asset as login). */
export const BRAND_LOGO_SRC = require("./assets/logo.png");
